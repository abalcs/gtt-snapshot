rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all documents (public reference guide)
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
